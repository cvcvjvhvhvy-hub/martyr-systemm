# دليل نقل وتشغيل التطبيق على جهاز آخر

## 📁 الملفات المطلوبة للنقل

### ✅ الملفات الأساسية (يجب نقلها):
```
flutter_app/
├── lib/                    # كود التطبيق
├── android/               # ملفات Android
├── pubspec.yaml           # Dependencies
├── analysis_options.yaml # إعدادات التحليل
├── .gitignore            # ملفات Git
├── .metadata             # معلومات Flutter
└── README.md             # التوثيق
```

### ❌ الملفات التي لا تُنقل:
- `.dart_tool/`
- `build/`
- `.flutter-plugins`
- `.flutter-plugins-dependencies`
- `.packages`
- `pubspec.lock` (سيتم إنشاؤه تلقائياً)

## 🔧 خطوات التشغيل على الجهاز الجديد

### 1. متطلبات النظام:
```bash
# تأكد من تثبيت Flutter
flutter --version

# تأكد من تثبيت Android SDK
flutter doctor
```

### 2. بعد نقل الملفات:
```bash
# الانتقال لمجلد التطبيق
cd flutter_app

# تحديث Dependencies
flutter pub get

# تنظيف البناء السابق
flutter clean

# إعادة البناء
flutter pub get
```

### 3. إعداد Firebase:
- استبدل `android/app/google-services.json` بملف Firebase الحقيقي
- أو استخدم البيانات التجريبية الموجودة

### 4. تشغيل التطبيق:
```bash
# تشغيل على محاكي/جهاز
flutter run

# أو بناء APK
flutter build apk
```

## ⚠️ مشاكل شائعة وحلولها

### المشكلة: "Package not found"
```bash
flutter pub get
flutter pub deps
```

### المشكلة: "Android SDK not found"
```bash
flutter doctor
# اتبع التعليمات لإعداد Android SDK
```

### المشكلة: "Firebase configuration"
- تأكد من وجود `google-services.json` في `android/app/`
- أو استخدم البيانات التجريبية (admin/123456)

## 📋 قائمة التحقق قبل النقل

- ✅ جميع ملفات `lib/` موجودة
- ✅ ملف `pubspec.yaml` موجود
- ✅ مجلد `android/` مكتمل
- ✅ ملفات الإعداد (.metadata, analysis_options.yaml) موجودة
- ✅ لا توجد مجلدات build أو .dart_tool

## 🎯 النتيجة المتوقعة

بعد اتباع هذه الخطوات، يجب أن يعمل التطبيق بدون مشاكل على الجهاز الجديد.

**ملاحظة**: التطبيق جاهز للنقل والتشغيل على أي جهاز يحتوي على بيئة Flutter مُعدة بشكل صحيح.